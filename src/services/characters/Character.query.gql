query CharacterQuery($characterId: uuid!) {
  character: characters_by_pk(id: $characterId) {
    id
    name
    features
    maxHp: max_hp
    currentHp: current_hp
    picture
    description
    is_npc
    inventory
    equipment
  }
}
